# Companion library: the paper's controller needs the dynamics API added in
# v0.3.0 (compute_effective_arm_inertia, compute_coriolis_term). The released
# 0.2.0 does NOT contain it.
space-robot-dq @ git+https://github.com/HJahanshahi/space-robot-dq@v0.3.0

numpy>=1.24
scipy>=1.10
torch>=2.0
matplotlib>=3.7
tqdm
